# v0.0.44

## Changes

- [x] Added: OpenAI Responses API compaction support — `context_management` param with `compact_threshold` for automatic server-side context compaction in long-running conversations
- [x] Added: `OpenAICompactionOutput` and `OpenAIResponsesCompactionInputItem` types for compaction output/input items
- [x] Added: Compaction items are automatically preserved in `metadata.openai.compactionItems` and re-injected as input items on subsequent requests
- [x] Added: Streaming support for compaction items via `ResponsesStreamState` and `transformStreamEvent`
- [x] Added: Unit tests for `context_management` param forwarding and compaction output roundtrip
- [x] Added: Live test for `previous_response_id` chaining — verifies `response_id` metadata across multi-turn conversations
- [x] Added: Live test for auto-compaction — verifies compaction items are emitted with low `compact_threshold` across chained turns
- [x] Added: `llms.md` documentation for server-side conversation chaining (`previous_response_id`) and auto-compaction (`context_management`)
- [x] Added: `llms.md` sections for xAI provider options (3 API modes, agent tools, image gen) and Responses (OpenResponses spec) provider
- [x] Added: `llms.md` SSE keepalive docs and `retryAfterStrategy` retry strategy
- [x] Fixed: `llms.md` version bump 0.0.40 → 0.0.44, provider count 9+ → 11, capability matrix updated with Video/Audio columns
- [x] Fixed: README custom `PubSubAdapter` example missing `clear()` and `getCursorBase()` methods
